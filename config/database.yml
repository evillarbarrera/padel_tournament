default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5

development:
  <<: *default
  database: padel_development

test:
  <<: *default
  database: padel_test

production:
  url: <%= ENV['DATABASE_URL'] %>
