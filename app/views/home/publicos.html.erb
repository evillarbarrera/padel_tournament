<style>


.campeonatos-lista {
  background-image: url('https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=1350&q=80');
  background-size: cover;
  background-position: center;
  padding: 2.5rem 1rem 4rem;
  display: flex;
  flex-direction: column;
  gap: 1.8rem;
  min-height: 80vh;
}


.banner-campeonatos {
  background: rgba(255, 255, 255, 0.85);
  color: #007bff;
  text-align: center;
  padding: 1.8rem 1rem;
  font-size: 2.4rem;
  font-weight: 700;
  border-radius: 15px 15px 0 0;
  box-shadow: 0 4px 12px rgba(0, 123, 255, 0.3);
  margin-bottom: 1rem;
  background-image: url('https://images.unsplash.com/photo-1517649763962-0c623066013b?auto=format&fit=crop&w=1350&q=80');
  background-size: cover;
  background-position: center;
  position: relative;
  overflow: hidden;
}

.banner-campeonatos::before {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(255, 255, 255, 0.85);
  z-index: 0;
}

.banner-campeonatos h2 {
  position: relative;
  z-index: 1;
  margin: 0;
}


.campeonato-card {
  display: flex;
  background: rgba(255, 255, 255, 0.95);
  border-radius: 15px;
  box-shadow: 0 6px 18px rgba(0,0,0,0.15);
  overflow: hidden;
  max-width: 900px;
  margin: 0 auto;
  height: 160px;
  align-items: center;
  gap: 1.5rem;
  padding: 0 1rem;
  transition: transform 0.25s ease;
}

.campeonato-card:hover {
  transform: translateY(-6px);
}


.campeonato-img {
  width: 160px;
  height: 140px;
  border-radius: 15px;
  object-fit: cover;
  box-shadow: 0 3px 10px rgba(0,0,0,0.15);
  flex-shrink: 0;
}

.contenido-campeonato {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  color: #003366;
}


.titulo {
  font-size: 1.8rem;
  font-weight: 700;
  margin-bottom: 0.4rem;
}


.dato {
  font-size: 1.1rem;
  margin: 0.15rem 0;
}


.btn-inscribirse {
  margin

</style>
<!-- Banner -->
<div class="banner-campeonatos">
  <h2>🎾 Busca tu campeonato</h2>
</div>

<!-- Contenedor general -->
<div class="campeonatos-lista">
  <% @campeonatos.each do |campeonato| %>
    <div class="campeonato-card">
      <% if campeonato.foto.attached? %>
        <%= image_tag campeonato.foto, class: "campeonato-img" %>
      <% else %>
        <%= image_tag 'https://golfsantodomingo.cl/wp-content/uploads/2023/01/torneo-padel.png', class: "campeonato-img" %>
      <% end %>

      <div class="contenido-campeonato">
        <h3 class="titulo"><%= campeonato.nombre %></h3>
        <p class="dato"><strong>Tipo:</strong> <%= campeonato.tipo %></p>
        <p class="dato"><strong>Fecha inicio:</strong> <%= campeonato.fecha_inicio.strftime('%d/%m/%Y') %></p>
        <p class="dato"><strong>Club:</strong> <%= campeonato.club&.nombre %></p>

        <%= link_to "Inscribirse", new_inscripcione_path(campeonato_id: campeonato.id), class: "btn-inscribirse" %>
      </div>
    </div>
  <% end %>
</div>
